import 'bootstrap';
import Vue from 'vue';

// eslint-disable-next-line no-unused-vars
const advantages = new Vue({
  el: '#advantages',
  data() {
    return {
      advantages: 'Этот текст сгенерирован в блоке advantages при помощи Vue',
    };
  },
});
// eslint-disable-next-line no-unused-vars
const news = new Vue({
  el: '#news',
  data() {
    return {
      news: 'Этот текст сгенерирован в блоке news при помощи Vue',
    };
  },
});
